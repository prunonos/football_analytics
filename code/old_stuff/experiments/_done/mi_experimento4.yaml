experiment: own
experiment_id: exp_1
description: "replica de los experimentos de mi Tésis"
data:
  aggregations:
    Points: mean
    FTG: mean
    FTG_rival: mean
    # S: mean
    # ST: mean
    # F: mean
    # C: mean
    # Y: mean
    # R: mean
    # O: mean
    # HW: mean
    Side: mean
  features:
    -
  leagues:
    -
  lags:
    - 30D
    - 365D
    - 1825D
  min_samples:
    # - 1
    - 2
    - 10
    - 15
    # - 15
  col_group: 
    - "idTeam"
  col_window: "Date"
  balance: False
  factor: False
  sample: "random"
  split_date: "2017-01-01"
  save_data: True
paths:
  data: "datasets/data_train/data_"
  raw_data: "datasets/raw_datasets/datalake.csv"
optimization:
  iterations: 1
training:
  max_epochs: 
    - 10
  batch_size:
    - 4
  lr:
    - 0.01
    - 0.01